-- Conky, a system monitor https://github.com/brndnmtthws/conky
--
-- This configuration file is Lua code. You can write code in here, and it will
-- execute when Conky loads. You can use it to generate your own advanced
-- configurations.
--
-- Try this (remove the `--`):
--
--   print("Loading Conky config")
--
-- For more on Lua, see:
-- https://www.lua.org/pil/contents.html

conky.config = {
    alignment = 'top_left',
    background = true,
    border_width = 1,
	border_inner_margin = 5,
    cpu_avg_samples = 2,
    default_color = 'white',
    default_outline_color = 'white',
    default_shade_color = 'white',
    double_buffer = true,
    draw_borders = false,
    draw_graph_borders = true,
    draw_outline = false,
    draw_shades = false,
    extra_newline = false,
    font = 'Fantasque Sans Mono:size=22',
    gap_x = 60,
    gap_y = 60,
    minimum_height = 5,
    minimum_width = 5,
    net_avg_samples = 2,
    no_buffers = true,
    out_to_console = false,
    out_to_ncurses = false,
    out_to_stderr = false,
    out_to_x = true,
    own_window = true,
	own_window_argb_visual = true,
	own_window_argb_value = 100,
    own_window_class = 'Conky',
    own_window_type = 'desktop',
    show_graph_range = false,
    show_graph_scale = false,
    stippled_borders = 0,
    update_interval = 600000,
    uppercase = true,
    use_spacer = 'none',
    use_xft = true,
}

conky.text = [[
${color grey} Navigate Focus:

	${color grey} focus window right = CMD + E
	${color grey} focus window left = CMD + N
	${color grey} focus screen left = ALT + COMMA
	${color grey} focus screen right = ALT + period
	${color grey} jump to tag = CMD + number

$hr

${color grey} Move Windows:

	${color grey} move window to left screen = Alt + shift + comma
	${color grey} move window to right screen = Alt + shift + comma
	${color grey} resize window = MOD + right mouse click
	${color grey} move window = MOD + left mouse click

$hr

${color grey} Change Layout:

	${color grey} tiling layout = MOD + t
	${color grey} monocle layout = MOD + m
	${color grey} floating layout = MOD + f

$hr

${color grey} Open Programs:

	${color grey} open alacritty = CMD + ALT + T
	${color grey} open brave = CMd + ALT + W
	${color grey} open rofi = cmd + alt + R
	${color grey} open slack = cmd + alt + S

$hr

${color grey} Kitty Controls:

	${color grey} page up = CTRL + UP
	${color grey} page down = CTRL + DOWN
	${color grey} copy = CTRL + SHIFT + C
	${color grey} paste = CTRL + SHIFT + V
	${color grey} OPEN URL = CTRL + SHIFT + E
	${color grey} scroll up = CTRL + SHIFT + UP
	${color grey} scroll down = CTRL + SHIFT + DOWN
	${color grey} next window = CTRL + SHIFT + o
	${color grey} previous window = CTRL + SHIFT + h
	${color grey} open new pane = CTRL + SHIFT + ENTER
	${color grey} close current pane = CTRL + SHIFT + d
]]
